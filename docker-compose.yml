version: '3.7'

services:
  web:
    image: portal_web
    container_name: portal_web_container
    ports:
      - 80:80
    volumes:
      - ./volumes/web/log:/var/log/nginx
  web_api:
    image: portal_web-api
    container_name: portal_web-api_container
    environment:
      - REDIS_HOST
    ports:
      - 3000:3000
    volumes:
      - ./volumes/web_api/log:/var/log/express
    networks:
      - default
      - common_network
  redis:
    image: portal_redis
    container_name: portal_redis_container
    ports:
      - 6379:6379
    volumes:
      - ./volumes/redis/data:/data
      - ./volumes/redis/log:/var/log/redis:z
networks:
  common_network:
    external: true
